# Сначало определяет количество индексов, последующим к этим индексам присваевает переменную.

a = ['spam', 'eggs', 'hand', 'box']

for i in range(int(len(a))):
    print("index" " " + str(i) + " "'match'" " + a[i], end='\n')
